# Git Green Screen Extension

A VSCode Extension interface for the `git-new` subcommand to easily bootstrap projects from templates.

## Features

- Bootstrap repository using CLI command.
- Integrated VSCode prompts for project name and templates.
